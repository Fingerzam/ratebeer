<p id="notice"><%= notice %></p>

<p>
  <h2>Styles</h2>
</p>

<p>
  <ul>
    <% @styles.each do |style| %>
      <li><%= link_to style.name, style %></li>
    <% end %>
  </ul>
</p>

<%= link_to 'Create a new style', new_style_path,
            class: "btn btn-primary" unless current_user.nil? %>
